// code written by Elena

#include "CF.h"
#include <stdlib.h>
#include <stddef.h>

using namespace std;

CF::CF(size_t list_size, int capacity, int curr_level) {
    list_size = list_size;
    capacity = capacity;
    curr_level = curr_level;
    full = false;
    empty = true;
    
}