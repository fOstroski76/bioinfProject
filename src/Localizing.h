// code written by Elena

#include <string>
#include "CF.h"

using namespace std;

class Localize {
    public:
        int get_location(string &str);
        void recover_xi_x(string &str, CF* node);
};